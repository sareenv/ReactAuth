import React from 'react';
import 'antd/dist/antd.css';

// this thing doesn't need to be connected but it's there
class Security extends React.Component {

    render() {
        return(
            <div>
                <h3> Security Settings</h3>
            </div>
        )
    }
}

export default Security
